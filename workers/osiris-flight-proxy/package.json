{
  "name": "osiris-flight-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker flight feed proxy for OSIRIS v2.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260614.0",
    "wrangler": "^4.50.0"
  }
}
